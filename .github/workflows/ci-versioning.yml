# Staging proxy workflow for CI_Toolkit — copy to .github/workflows/ci-versioning.yml in your
# staging org's proxy repo (e.g. BuroHappoldEngineeringSandbox/CI_Toolkit).
#
# Points to @develop — use only in staging orgs. Production proxies use templates/ci-versioning.yml (@main).
#
# Required by rulesets: beta

name: Versioning

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
  cancel-in-progress: true

jobs:
  versioning:
    uses: BuroHappoldEngineeringAdmin/CI_Toolkit/.github/workflows/ci-versioning.yml@develop
    secrets: inherit
